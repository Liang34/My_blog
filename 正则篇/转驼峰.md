```js
var s1 = "get-element-by-id"// 转化为 getElementById

const transform = (str) => {
  // -\w表示匹配-以及其后面的一个字符
  const reg = /-\w/g
  str.replace(reg, function(x) {
    // x.slice(1)表示出去-然后再将第一个字母大写
    x.slice(1).toUpperCase()
  })
}
transform(s1)
```

