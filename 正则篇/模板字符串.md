```js
let template = '我是{{name}}，年龄{{age}}，性别{{sex}}';
let data = {
  name: '姓名',
  age: 18
}
render(template, data); // 我是姓名，年龄18，性别undefined
function render(template, data) {
  const reg = /\{\{(\w+)\}\}/
  if (reg.test(template)) {
    const key = reg.exec(template)[1]
    template = template.replace(reg, data[key])
    return render(template, data)
  }
  return template
}
```
```js

const template = (str, data) => {
  const reg = /\$\{(\w+)\}/
  if(reg.test(str)) {
    const name = reg.exec(str)[1]
    str = str.replace(reg, data[name])
    return template(str, data)
  }
  return str
}
console.log(template("${name} is  ${age} years old",{name:"Jim",age:20}))
console.log(template('asdd ${tgy}  has ${gender}',{tgy:'tgy',gender:'man'}))
```
```js
var tpl = "hello: <%=name%>, age: <%=age%>";
const compiled = function (str, data) {
  const reg = /\<\%\=(\w+)\%\>/
  // \s表示空格\s*表示有0个或多个空格:如果是<%= name %>的话
  // reg = /\<\%\=\s*(\w+)\s*\%\>/
  if (reg.test(str)) {
    const name = reg.exec(str)[1]
    str = str.replace(reg, data[name])
    return compiled(str, data)
  }
  return str
}
console.log(compiled(tpl, {
  name: 'Kevin',
  age: '12'
}))
```