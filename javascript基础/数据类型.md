```js
// 任意值与布尔值比较，都会将两边的值转化为Number。
if ([] == false) {console.log(1);};// 输出
// // [] ---> '' ---> 0
// false ---> 0
if ({} == false ) {console.log(2);};//不输出
// {}--->[object Object]--->1
// false--->0
// 数组arr中没有元素，也是一个object
// 既然是object，用于判断条件时就会被转化为true
if ([]) {console.log(3);};// 输出

if ([1] == [1]) {console.log(4);};
```